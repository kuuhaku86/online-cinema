services:
  backend:
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules

  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6380:6379"

  hate-speech-detector:
    ports:
      - "5000:5000"
